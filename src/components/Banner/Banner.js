import './Banner.css';

export const Banner = () => {
    // JSX
    return(
        <header className="cmp-banner">
            <img src="./imagens/banner.png" alt="banner principal da página do Organo" />
        </header>
    )
}